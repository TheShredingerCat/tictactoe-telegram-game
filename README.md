# 🎮 Tic-Tac-Toe Telegram Game  
Полноценная мини-игра для Telegram, разработанная с нуля:  
фронтенд, бэкенд, Telegram Bot и инфраструктура — всё полностью контейнеризировано.

Игрок соревнуется против AI.  
При победе генерируется уникальный 5-значный промокод, который выводится на экран и отправляется пользователю в Telegram.  
При поражении показывается отдельный экран и отправляется сообщение «Проигрыш».

## 🚀 Особенности проекта

### 🔹 Реализация через **Telegram Game API**
Бот отправляет GameMessage, игра открывается внутри Telegram WebView.  
Поддержаны оба режима: WebApp и GameProxy.

### 🔹 AI-противник  
Лёгкий и непротиворечивый алгоритм с приоритетами:
1. Выигрышный ход  
2. Блокировка игрока  
3. Центр  
4. Углы  
5. Любая доступная клетка  

### 🔹 Промокоды с гарантированной уникальностью  
При победе создаётся 5-значный код.  
Бэкенд гарантирует уникальность (SQL-уровень + проверка перед вставкой).  
Сервис разделён на promo_service / telegram_service / security_service.

### 🔹 Красивый UI для женской аудитории (25–40)
Плавные пастельные градиенты, 3D-кнопки-пилюли, аккуратные карточки победы/проигрыша.  
Вся визуализация адаптирована под Telegram WebView.

### 🔹 Чистая архитектура
- Модули фронтенда изолированы: core / ui / telegram  
- Бэкенд — FastAPI + сервисный слой + SQLAlchemy ORM  
- Telegram Bot работает параллельно с backend (async)

### 🔹 Полная контейнеризация
Nginx раздаёт фронтенд, FastAPI работает как API, Telegram Bot запускается в фоне.  
Готовый `docker-compose.yml` для запуска в два шага.

---

# 🧱 Архитектура
tictactoe-telegram-game/
├── backend/
│ ├── app/
│ │ ├── api/ # REST-эндпоинты
│ │ ├── bots/ # Telegram Bot (Game API)
│ │ ├── models/ # SQLAlchemy ORM
│ │ ├── services/ # Бизнес-логика (promo, security, telegram)
│ │ ├── db/ # Session, Base, migrations placeholder
│ │ ├── main.py # Точка входа FastAPI + Bot
│ │ └── tests/ # Unit-тесты
│ └── pyproject.toml
│
├── frontend/
│ ├── index.html
│ ├── assets/ # X/O icons, buttons, promo graphics
│ ├── src/
│ │ ├── core/ # gameEngine, botAI, types
│ │ ├── ui/ # boardView, modals, layout, theme.css
│ │ ├── telegram/ # integration with Telegram WebApp/GameProxy
│ │ └── index.ts # фронтовый "дирижёр" проекта
│
├── infra/
│ ├── nginx.conf # Статика + SPA routing
│ └── env.example
│
└── docker-compose.yml

# 🧩 Технологический стек

### Frontend
- TypeScript  
- чистая модульная архитектура (core/ui/telegram)  
- Telegram Games API  
- адаптивный UI под WebView  
- Nginx для раздачи

### Backend
- FastAPI  
- SQLAlchemy 2.0  
- Pydantic v2  
- python-telegram-bot 21.x  
- Async IO + background tasks  
- SQLite (для тестового) / PostgreSQL (production ready)

### Инфраструктура
- Docker  
- Nginx SPA routing  
- Чистая конфигурация окружения (.env)

